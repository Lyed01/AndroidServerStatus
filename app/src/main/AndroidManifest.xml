<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lyed.minecraftserverstatus">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@drawable/eye"
        android:label="@string/app_name"
        android:roundIcon="@drawable/eye"
        android:supportsRtl="true"
        android:theme="@style/Theme.MinecraftServerStatus"
        tools:ignore="ExtraText"
        tools:targetApi="tiramisu">



        <!-- SplashActivity -->
        <activity
            android:name="com.lyed.minecraftserverstatus.ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name="com.lyed.minecraftserverstatus.ui.LoginActivity"
            android:exported="true">
        </activity>


        <!-- AddServerActivity -->
        <activity
            android:name="com.lyed.minecraftserverstatus.ui.AddServerActivity"
            android:exported="true"
            android:theme="@style/CustomDialogTheme" />

        <!-- ManageServersActivity -->
        <activity
            android:name="com.lyed.minecraftserverstatus.ui.ManageServersActivity"
            android:exported="true"
            android:label="@string/title_activity_manage_servers" />

    </application>

</manifest>
